<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Cor de fundo do círculo -->
    <solid android:color="@color/colorAccentDespesa" />

    <!-- Opcional: borda branca fina -->
    <stroke
        android:width="1dp"
        android:color="@color/textPrimary" />

    <!-- Opcional: padding interno -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp"/>
</shape>